Local Node.js CLI and stdio MCP clients. They discover a running WanJuan desktop instance from a protected per-launch credential file, skip stale processes or ports, and call a bearer-authenticated HTTP service bound only to 127.0.0.1. The boundary exposes task status, model names, image/video submission, query, wait, and cancel; it does not expose API keys or arbitrary Electron IPC.
