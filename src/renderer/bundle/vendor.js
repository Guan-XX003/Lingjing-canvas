// —— 真实 npm 依赖桥 ——
//
// 原 vendor.js 是 491KB 的第三方库打包压缩副本（react / react-dom / react/jsx-runtime /
// @xyflow/react / lucide-react / localforage / dagre / react-image-crop）。
// 本文件以完全相同的导出名，从 package.json 声明的真实 npm 依赖再导出，
// bundle/index.js 的 import 无需任何改动。
//
// 六个「模块工厂」导出（Ct/St/bt/xt/a/t）保持原 rolldown CJS 懒加载器形状：
// 调用返回该模块的导出对象，供 rolldown-runtime 的 __toESM(e(x(), 1)) 消费。
import * as WanjuanReact from "react";
import * as WanjuanReactDom from "react-dom";
import * as WanjuanReactDomClient from "react-dom/client";
import * as WanjuanJsxRuntime from "react/jsx-runtime";
import localforage from "localforage";
import dagre from "dagre";
import ReactCrop, { centerCrop, makeAspectCrop } from "react-image-crop";
import {
  ArrowUp,
  Palette,
  Inbox,
  Redo2,
  Pencil,
  Pipette,
  Maximize2,
  FileText,
  Hash,
  ListPlus,
  Copy,
  Download,
  Link,
  MonitorPlay,
  Minimize2,
  Pen,
  Mic,
  Check,
  Link2,
  RefreshCw,
  Play,
  FolderOpen,
  Image,
  Film,
  CirclePlay,
  Circle,
  CircleAlert,
  Sparkles,
  Send,
  X,
  Undo2,
  Type,
  Square,
  Star,
  Music,
  PenLine,
  Upload,
  Trash2,
  ZoomIn,
  Trash,
  Crop,
  Zap,
  Undo,
  Settings,
  Puzzle,
  Save,
  Settings2,
  LayoutGrid,
} from "lucide-react";
import {
  Position,
  BackgroundVariant,
  Handle,
  Panel,
  Background,
  MiniMap,
  Controls,
  ReactFlow,
  NodeToolbar,
  ReactFlowProvider,
  NodeResizer,
  BaseEdge,
  useNodesState,
  useEdgesState,
  useNodeConnections,
  useUpdateNodeInternals,
  useReactFlow,
  useOnViewportChange,
  useNodesData,
  addEdge,
  getBezierPath,
} from "@xyflow/react";

// 模块工厂：调用即返回该模块的导出对象（与原打包副本的 CJS 懒加载器行为一致）。
const wanjuanReactFactory = () => WanjuanReact;
const wanjuanReactDomFactory = () => WanjuanReactDom;
const wanjuanJsxRuntimeFactory = () => WanjuanJsxRuntime;
const wanjuanReactDomClientFactory = () => WanjuanReactDomClient;
const wanjuanLocalforageFactory = () => localforage;
const wanjuanDagreFactory = () => dagre;

export {
  // —— 模块工厂 ——
  wanjuanReactFactory as Ct,
  wanjuanReactDomFactory as St,
  wanjuanJsxRuntimeFactory as bt,
  wanjuanReactDomClientFactory as xt,
  wanjuanLocalforageFactory as a,
  wanjuanDagreFactory as t,
  // —— @xyflow/react ——
  Position as _t,
  BackgroundVariant as tt,
  Handle as at,
  Panel as ct,
  Background as et,
  MiniMap as ot,
  Controls as rt,
  ReactFlow as ut,
  NodeToolbar as it,
  ReactFlowProvider as lt,
  NodeResizer as st,
  BaseEdge as nt,
  useNodesState as dt,
  useEdgesState as mt,
  useNodeConnections as ft,
  useUpdateNodeInternals as gt,
  useReactFlow as ht,
  useOnViewportChange,
  useNodesData as pt,
  addEdge as vt,
  getBezierPath as yt,
  // —— react-image-crop ——
  ReactCrop as i,
  makeAspectCrop as n,
  centerCrop as r,
  // —— lucide-react 图标 ——
  ArrowUp as $,
  Palette as A,
  Inbox as B,
  Redo2 as C,
  Pencil as D,
  Pipette as E,
  Maximize2 as F,
  FileText as G,
  Hash as H,
  ListPlus as I,
  Copy as J,
  Download as K,
  Link as L,
  MonitorPlay as M,
  Minimize2 as N,
  Pen as O,
  Mic as P,
  Check as Q,
  Link2 as R,
  RefreshCw as S,
  Play as T,
  FolderOpen as U,
  Image as V,
  Film as W,
  CirclePlay as X,
  Circle as Y,
  CircleAlert as Z,
  Sparkles as _,
  Send as b,
  X as c,
  Undo2 as d,
  Type as f,
  Square as g,
  Star as h,
  Music as j,
  PenLine as k,
  Upload as l,
  Trash2 as m,
  ZoomIn as o,
  Trash as p,
  Crop as q,
  Zap as s,
  Undo as u,
  Settings as v,
  Puzzle as w,
  Save as x,
  Settings2 as y,
  LayoutGrid as z,
};
