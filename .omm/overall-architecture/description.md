WanJuan Lingjing is an Electron desktop application whose renderer hosts a React Flow multimedia AI canvas. A trusted preload bridge connects the UI to main-process networking, files, project assets, local tools, updates, workspace services, and an account identity boundary that keeps refresh and enterprise workspace tokens in the main process.
