Jixin default configuration is a catalog and protocol preset rather than a separate forwarding layer. User configuration can override defaults, and advanced Tianji settings can become independently authoritative after manual edits. Enterprise team prompt content stays on the local enterprise gateway: member access uses a workspace token, host access uses a main-process-verified session, and account-service cloud prompt workspaces remain a separate integration.
